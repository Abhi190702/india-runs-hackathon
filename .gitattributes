# Normalize line endings: store text as LF in the repo, check out natively.
# Prevents the "LF will be replaced by CRLF" warnings on Windows and keeps
# diffs clean across the team's machines.
* text=auto eol=lf
*.py text eol=lf
*.md text eol=lf
*.yaml text eol=lf
*.txt text eol=lf
